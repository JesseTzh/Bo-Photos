import { Button,Form,Input,Typography,message } from "antd";import { useChangePassword } from "../features/auth/api";
export function AdminAccountPage(){const change=useChangePassword(),[msg,holder]=message.useMessage();return <div className="admin-page-stack">{holder}<Typography.Title>账户</Typography.Title><Form layout="vertical" onFinish={async v=>{await change.mutateAsync(v);msg.success("密码已修改，请重新登录")}}><Form.Item name="current_password" label="当前密码" rules={[{required:true}]}><Input.Password/></Form.Item><Form.Item name="new_password" label="新密码" rules={[{required:true,min:12}]}><Input.Password/></Form.Item><Button type="primary" htmlType="submit">修改密码</Button></Form></div>}
